make list of users
create secondary list
check against first list
outliers are new people who have joined
check if outliers have join message via opening a text file that saves these
    users can create their own join message using !join message "<message>"
if they do print it out to chat
